{
  "permissions": {
    "allow": [
      "Bash(python3 -c \"\nimport sys\nlines = sys.stdin.readlines()\nfor line in lines:\n    if ''''passed'''' in line.lower() or ''''failed'''' in line.lower() or ''''some tests'''' in line.lower() or ''''+'''' in line[:6]:\n        print(line.rstrip())\")"
    ],
    "deny": [],
    "ask": []
  }
}
